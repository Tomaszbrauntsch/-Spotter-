import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

//we need to code here: if number is smaller than .5(?) display results: opt1; if not display results: opt2